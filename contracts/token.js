// contracts/token.js

//const web3 = require('../web3');
//const tokenABI = require('../abi/token.json'); // ABI del contrato
//const contractAddress = 'DIRECCIÓN_DEL_CONTRATO';

//const tokenContract = new web3.eth.Contract(tokenABI, contractAddress);

//exports.transferTokens = async (from, to, amount) => {
    //const gasPrice = await web3.eth.getGasPrice();
    //const transaction = tokenContract.methods.transfer(to, amount);
    //onst gas = await transaction.estimateGas({ from });

    //return transaction.send({ from, gasPrice, gas });
//};

// Añade más funciones según sea necesario
